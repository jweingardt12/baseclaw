"""season — Yahoo Fantasy Baseball In-Season Manager package.

Helpers are in _helpers.py. Domain modules (lineup, trades, etc.) will
import from _helpers directly. The wildcard re-export will be added once
season-manager.py is updated to import from this package.
"""
